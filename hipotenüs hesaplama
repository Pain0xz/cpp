#include <iostream>
#include <ctime>
#include <windows.h>
#include <stdio.h>
#include <locale.h>
#include <string.h>
#include <math.h>
#pragma warning (disable:4996)
using namespace std;

int main() {
	int k1, k2;
	cout << "1. kenari gir\n--> ";
	cin >> k1;
	cout << "2. kenari gir\n--> ";
	cin >> k2;
	int x = pow(k1, 2) + pow(k2, 2);
	cout << "hiponotus: " << sqrt(x);
	return 0;
}
